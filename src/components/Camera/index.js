
    // useFrame((state) => {
    //     if (cam.current) {
    //         delta += 0.01
    //         // const angle = Math.sin(state.clock.elapsedTime * rotationSpeed) * maxRotationAngle;
    //         cam.current.position.x = Math.cos(delta) * 7;
    //         cam.current.position.z = Math.sin(delta) * 10;
    //     }
    // });